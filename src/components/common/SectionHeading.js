const SectionHeading = ({ heading }) => {
    return (
        <h3 className="sectionHeading text-center text-uppercase d-flex gap-3 align-items-center justify-content-center fw-bold">{heading}</h3>
    )
}
export default SectionHeading;